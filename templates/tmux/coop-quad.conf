# tmux layout for 4-station co-op session (2x2 grid)
# Usage: tmux new-session -s coop \; source-file templates/tmux/coop-quad.conf

# Create 4 panes in a grid
split-window -h
split-window -v
select-pane -t 0
split-window -v

# Tile evenly
select-layout tiled
